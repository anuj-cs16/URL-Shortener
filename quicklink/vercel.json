{
  "version": 2,
  "buildCommand": "cd client && npm install --legacy-peer-deps && CI=false GENERATE_SOURCEMAP=false npm run build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/api/index.js"
    }
  ]
}
